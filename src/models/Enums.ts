export enum SidebarCondition {
  category = "category",
  brand = "brand",
  rating = "rating",
}

export enum SortCondition {
  title = "title",
  price = "price",
  discount = "discount",
  stars = "stars",
  delivery = "delivery",
  stock = "stock",
}

export enum SortByNumbers {
  discount = "discount",
  popularity = "popularity",
  stock = "stock",
}

export enum SortByStrings {
  name = "name",
  delivery = "delivery",
}
