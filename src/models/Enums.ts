export enum SidebarCondition {
  category = "category",
  brand = "brand",
  rating = "rating",
}

export enum SortCondition {
  title = "title",
  price = "price",
  discount = "discount",
  stars = "stars",
  delivery = "delivery",
  stock = "stock",
}
